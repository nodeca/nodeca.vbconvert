- self.add_raw_data('forum_images_task', self.task_info);

#vbconvert-task-forum-images.vbconvert-widget
  h4
    = self.t('caption')
    = ' '
    small(data-bind='visible: started, text: (current() / total() * 100 || 0).toFixed(2) + "%"'
    )

  progress.progress(
    value=0
    max=100
    data-bind='value: (current() / total() * 100) || 0'
  )

  div
    button.btn.btn-default(
      data-on-click='admin.vbconvert.blocks.forum_images.start'
      data-bind='visible: !started()'
    )= self.t('start')

    button.btn.btn-danger(
      data-on-click='admin.vbconvert.blocks.forum_images.stop'
      data-bind='visible: started'
      style='display: none'
    )= self.t('stop')
